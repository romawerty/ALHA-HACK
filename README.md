# Умный Ассистент для Управления Встречами и Почтой

Система микросервисов для автоматизации управления календарем, почтой и новостями с AI-агентом.

## 🏗️ Архитектура

Система состоит из следующих независимых микросервисов:

- **API Gateway** (порт 8000): Единая точка входа для фронтенда, маршрутизирует запросы к соответствующим сервисам
- **Auth Service** (порт 8001): Отвечает за аутентификацию пользователя и выдачу JWT-токенов
- **Calendar Service** (порт 8002): Интегрируется с Яндекс Calendar API, управляет событиями календаря
- **Email Service** (порт 8003): Интегрируется с Яндекс Почтой API, фильтрует и обрабатывает письма
- **News Service** (порт 8004): Парсит RSS-ленту RBC.ru и генерирует саммари новостей
- **LLM Agent Service** (порт 8005): AI-агент для анализа писем, автоматического создания встреч и отправки ответов
- **Frontend** (порт 8501): Streamlit приложение с интерфейсом управления

## 🚀 Быстрый старт

### Предварительные требования

**Вариант 1 (с Docker):**
- Docker и Docker Compose
- Аккаунт Яндекс (для получения API ключей)

**Вариант 2 (без Docker):**
- Python 3.11+
- pip
- Аккаунт Яндекс (для получения API ключей)

### Установка

1. **Клонируйте репозиторий** (или используйте текущую директорию)

2. **Создайте файл `.env`** на основе `env.example`:
   ```bash
   cp env.example .env
   ```

3. **Получите API ключи Яндекс**:
   - Следуйте инструкциям в файле `YANDEX_API_SETUP.md`
   - Заполните `YANDEX_CALENDAR_CLIENT_ID`, `YANDEX_CALENDAR_CLIENT_SECRET`
   - Заполните `YANDEX_EMAIL_CLIENT_ID`, `YANDEX_EMAIL_CLIENT_SECRET`

4. **Настройте JWT секрет** (обязательно измените в продакшене):
   ```env
   JWT_SECRET_KEY=ваш-длинный-секретный-ключ-минимум-32-символа
   ```

5. **Запустите систему**:

   **С Docker:**
   ```bash
   docker-compose up --build
   ```

   **Без Docker:**
   ```bash
   # Установите зависимости
   python install_dependencies.py
   
   # Запустите все сервисы
   python run_local.py
   ```

6. **Откройте приложение**:
   - Frontend: http://localhost:8501
   - API Gateway: http://localhost:8000

### ⚠️ Если что-то не работает

1. Запустите диагностику: `python check_system.py`
2. См. **FIX_PROBLEMS.md** для быстрого решения
3. См. **TROUBLESHOOTING.md** для подробной помощи
4. См. **START_WITHOUT_DOCKER.md** если Docker не установлен

## 📋 Функциональность

### Главная страница (3 колонки)

1. **Колонка 1: Предстоящие встречи**
   - Список ближайших событий из календаря
   - Встречи с важными контактами подсвечиваются красным
   - Кнопки создания и отмены встреч

2. **Колонка 2: Входящие письма**
   - Список писем от людей (исключены рассылки)
   - Письма от важных контактов подсвечиваются красным
   - Кнопка "Написать ответ" для каждого письма

3. **Колонка 3: Финансовые новости (RBK)**
   - Список последних новостей с саммари
   - Ссылки на полные статьи

### Дополнительные вкладки

- **Портфель рекомендаций**: Архив всех действий AI-агента
- **Настройки**: Управление важными контактами, игнорируемыми отправителями, рабочим расписанием и шаблонами ответов

## 🤖 Логика AI-агента

AI-агент автоматически:

1. **Анализирует новые письма** на предмет предложения о встрече
2. **Извлекает информацию**: дату, время, тему встречи
3. **Проверяет конфликты** через Calendar Service
4. **Действует**:
   - Если время свободно → создает событие и отправляет ответ согласия
   - Если время занято → предлагает альтернативные слоты из рабочего расписания
5. **Сохраняет все действия** в портфель рекомендаций

## 🔑 Получение API ключей

### Яндекс Календарь и Почта

Подробная инструкция в файле `YANDEX_API_SETUP.md`

**Краткая версия:**
1. Перейдите на https://oauth.yandex.ru/
2. Создайте новое приложение
3. Укажите Redirect URI: `http://localhost:8000/auth/yandex/callback`
4. Включите права: `calendar:read`, `calendar:write`, `mail:read`, `mail:write`
5. Скопируйте Client ID и Client Secret в `.env`

### Hugging Face API (опционально)

Для улучшенной работы LLM-агента:

1. Зарегистрируйтесь на https://huggingface.co/
2. Создайте API токен в настройках
3. Добавьте в `.env`: `HUGGINGFACE_API_KEY=ваш-токен`

Без API ключа система будет работать с упрощенной эвристикой.

## 📁 Структура проекта

```
.
├── docker-compose.yml          # Конфигурация всех сервисов
├── env.example                 # Пример файла с переменными окружения
├── README.md                   # Этот файл
├── YANDEX_API_SETUP.md         # Инструкция по получению API ключей
├── services/
│   ├── api-gateway/            # API Gateway сервис
│   │   ├── main.py
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   ├── auth-service/           # Сервис аутентификации
│   │   ├── main.py
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   ├── calendar-service/       # Сервис календаря
│   │   ├── main.py
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   ├── email-service/          # Сервис почты
│   │   ├── main.py
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   ├── news-service/           # Сервис новостей
│   │   ├── main.py
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   └── llm-agent-service/      # AI-агент
│       ├── main.py
│       ├── Dockerfile
│       └── requirements.txt
└── frontend/                   # Streamlit приложение
    ├── app.py
    ├── Dockerfile
    └── requirements.txt
```

## 🎨 Особенности интерфейса

- **Темная тема**: Переключатель в верхней панели
- **Адаптивный дизайн**: 3 колонки на главной странице
- **Модальные окна**: Для создания встреч и ответов на письма
- **Выход из системы**: Только через кнопку "Выход" (сессия сохраняется)

## ⚠️ Важные замечания

1. **Безопасность**: В продакшене обязательно:
   - Используйте хеширование паролей (bcrypt)
   - Измените JWT_SECRET_KEY на случайный длинный ключ
   - Используйте HTTPS
   - Настройте правильные CORS политики

2. **Хранилище данных**: В текущей версии используется файловое хранилище. Для продакшена рекомендуется использовать БД (PostgreSQL, MongoDB).

3. **API Яндекс**: Некоторые методы могут требовать дополнительной настройки в зависимости от версии API.

## 🐛 Устранение неполадок

- **Сервисы не запускаются**: Проверьте, что все порты свободны (8000-8005, 8501)
- **Ошибки авторизации**: Убедитесь, что API ключи Яндекс правильно настроены
- **Заглушки вместо данных**: Это нормально, если API ключи не настроены - система работает в демо-режиме

## 📝 Лицензия

Этот проект создан в образовательных целях.

