# 🔧 Быстрое решение проблем

## ❌ Проблема: "Не работает"

### Шаг 1: Диагностика

Запустите диагностический скрипт:
```powershell
python check_system.py
```

### Шаг 2: Решение в зависимости от проблемы

## 🐳 Если Docker не установлен

**Вариант А: Установить Docker**
1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop
2. Установите и запустите Docker Desktop
3. Затем запустите: `docker-compose up --build`

**Вариант Б: Запуск без Docker (локально)**
1. Установите зависимости:
   ```powershell
   python install_dependencies.py
   ```
2. Создайте файл `.env` (если еще не создан):
   ```powershell
   Copy-Item env.example .env
   ```
3. Запустите все сервисы:
   ```powershell
   python run_local.py
   ```
4. Откройте http://localhost:8501

## 📝 Если файл .env не создан

```powershell
Copy-Item env.example .env
```

Затем откройте `.env` и заполните:
- `JWT_SECRET_KEY` - придумайте длинный случайный ключ (минимум 32 символа)
- API ключи Яндекс (см. `YANDEX_API_SETUP.md`)

## 🔌 Если порты заняты

Найдите процесс:
```powershell
netstat -ano | findstr :8000
```

Остановите процесс или измените порты в `docker-compose.yml`

## 🚀 Быстрый старт (пошагово)

### С Docker:
```powershell
# 1. Создать .env
Copy-Item env.example .env

# 2. Запустить
docker-compose up --build
```

### Без Docker:
```powershell
# 1. Создать .env
Copy-Item env.example .env

# 2. Установить зависимости
python install_dependencies.py

# 3. Запустить
python run_local.py
```

## ✅ Проверка работы

1. Откройте http://localhost:8501
2. Нажмите "Регистрация"
3. Заполните форму и войдите
4. Должна открыться главная страница с 3 колонками

## 📚 Дополнительная помощь

- **TROUBLESHOOTING.md** - подробное руководство по устранению неполадок
- **START_WITHOUT_DOCKER.md** - запуск без Docker
- **QUICKSTART.md** - быстрый старт
- **YANDEX_API_SETUP.md** - настройка API ключей

## 🆘 Если ничего не помогает

1. Проверьте логи:
   ```powershell
   # С Docker
   docker-compose logs
   
   # Без Docker - смотрите вывод в терминалах
   ```

2. Убедитесь, что:
   - Python 3.11+ установлен
   - Все файлы на месте
   - Порты свободны
   - .env файл создан

3. Полная перезагрузка:
   ```powershell
   # С Docker
   docker-compose down -v
   docker-compose up --build
   
   # Без Docker
   # Остановите все процессы Python и запустите заново
   ```

