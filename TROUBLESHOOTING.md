# Устранение неполадок

## 🔍 Диагностика проблем

### Проблема 1: Docker не запускается

**Симптомы:**
- Ошибка `docker-compose: command not found`
- Ошибка `Cannot connect to the Docker daemon`

**Решения:**
1. Убедитесь, что Docker Desktop установлен и запущен
2. Проверьте: `docker --version` и `docker-compose --version`
3. Перезапустите Docker Desktop

### Проблема 2: Сервисы не запускаются

**Симптомы:**
- `ERROR: Service 'api-gateway' failed to build`
- `ModuleNotFoundError` или `ImportError`

**Решения:**
1. Проверьте, что все файлы на месте:
   ```bash
   ls services/api-gateway/
   ls services/auth-service/
   # и т.д.
   ```

2. Пересоберите контейнеры:
   ```bash
   docker-compose down
   docker-compose build --no-cache
   docker-compose up
   ```

3. Проверьте логи конкретного сервиса:
   ```bash
   docker-compose logs api-gateway
   docker-compose logs auth-service
   ```

### Проблема 3: Порт уже занят

**Симптомы:**
- `ERROR: bind: address already in use`
- `Port 8000 is already allocated`

**Решения:**
1. Найдите процесс, использующий порт:
   ```powershell
   # Windows PowerShell
   netstat -ano | findstr :8000
   ```

2. Остановите процесс или измените порты в `docker-compose.yml`

3. Остановите все контейнеры:
   ```bash
   docker-compose down
   ```

### Проблема 4: Ошибки подключения между сервисами

**Симптомы:**
- `Connection refused`
- `Service unavailable`
- `503 Service Unavailable`

**Решения:**
1. Убедитесь, что все сервисы в одной сети:
   ```bash
   docker network ls
   ```

2. Проверьте, что сервисы запущены:
   ```bash
   docker-compose ps
   ```

3. Проверьте переменные окружения:
   ```bash
   docker-compose config
   ```

### Проблема 5: Frontend не может подключиться к API

**Симптомы:**
- `Failed to fetch`
- `CORS error`
- Пустая страница

**Решения:**
1. Проверьте URL в `frontend/app.py`:
   - В Docker: `http://api-gateway:8000`
   - Локально: `http://localhost:8000`

2. Проверьте, что API Gateway запущен:
   ```bash
   curl http://localhost:8000/health
   ```

3. Проверьте логи фронтенда:
   ```bash
   docker-compose logs frontend
   ```

### Проблема 6: Ошибки аутентификации

**Симптомы:**
- `Invalid token`
- `401 Unauthorized`
- Не удается войти

**Решения:**
1. Проверьте JWT_SECRET_KEY в `.env`
2. Очистите данные и перезапустите:
   ```bash
   docker-compose down -v
   docker-compose up
   ```

### Проблема 7: Ошибки с Яндекс API

**Симптомы:**
- `Yandex Calendar API not configured`
- `Failed to get token`
- Заглушки вместо реальных данных

**Решения:**
1. Проверьте, что API ключи заполнены в `.env`
2. Следуйте инструкции в `YANDEX_API_SETUP.md`
3. Проверьте Redirect URI в настройках приложения Яндекс

### Проблема 8: Streamlit показывает ошибки

**Симптомы:**
- `ModuleNotFoundError: No module named 'streamlit'`
- Пустая страница с ошибками

**Решения:**
1. Проверьте зависимости:
   ```bash
   docker-compose exec frontend pip list
   ```

2. Пересоберите контейнер:
   ```bash
   docker-compose build frontend
   docker-compose up frontend
   ```

## 🛠️ Команды для диагностики

### Проверка статуса всех сервисов
```bash
docker-compose ps
```

### Просмотр логов всех сервисов
```bash
docker-compose logs
```

### Просмотр логов конкретного сервиса
```bash
docker-compose logs api-gateway
docker-compose logs frontend
```

### Проверка конфигурации
```bash
docker-compose config
```

### Перезапуск всех сервисов
```bash
docker-compose restart
```

### Полная пересборка
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up
```

### Очистка всех данных
```bash
docker-compose down -v
docker system prune -a
```

## 🔧 Проверка работоспособности

### 1. Проверка API Gateway
```bash
curl http://localhost:8000/health
```
Должен вернуть: `{"status":"ok","service":"api-gateway"}`

### 2. Проверка Auth Service
```bash
curl http://localhost:8001/health
```

### 3. Проверка регистрации
```bash
curl -X POST http://localhost:8000/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"test123","name":"Test User"}'
```

### 4. Проверка Frontend
Откройте в браузере: http://localhost:8501

## 📝 Частые ошибки и решения

### Ошибка: `'dict' object has no attribute 'get'`
**Причина:** Неправильная обработка ответа API  
**Решение:** Проверьте формат ответа от сервиса

### Ошибка: `Connection timeout`
**Причина:** Сервис не запущен или недоступен  
**Решение:** Проверьте `docker-compose ps` и логи

### Ошибка: `Invalid JSON`
**Причина:** Неправильный формат данных  
**Решение:** Проверьте структуру запроса/ответа

### Ошибка: `Port binding failed`
**Причина:** Порт уже используется  
**Решение:** Остановите процесс или измените порт

## 🆘 Если ничего не помогает

1. **Полная очистка и перезапуск:**
   ```bash
   docker-compose down -v
   docker system prune -a
   docker-compose build --no-cache
   docker-compose up
   ```

2. **Проверка файлов:**
   - Убедитесь, что все файлы на месте
   - Проверьте права доступа
   - Проверьте кодировку файлов (должна быть UTF-8)

3. **Проверка переменных окружения:**
   - Убедитесь, что `.env` файл существует
   - Проверьте синтаксис `.env` файла
   - Убедитесь, что нет лишних пробелов

4. **Логи:**
   - Сохраните логи всех сервисов
   - Проверьте ошибки в логах
   - Ищите ключевые слова: `ERROR`, `Exception`, `Traceback`

## 📞 Дополнительная помощь

Если проблема не решена:
1. Проверьте версии Docker и Docker Compose
2. Проверьте версию Python (должна быть 3.11+)
3. Проверьте системные требования
4. Создайте issue с описанием проблемы и логами

